# Mobile helper: placeholder for building the Capacitor app

This folder separates the mobile-specific scaffolding from the server repo root.

Use mobile/setup.sh to prepare the mobile/www folder from the root static/ directory, then use Capacitor commands to add platforms and build.
